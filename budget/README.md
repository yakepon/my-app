# 家計簿

支出の記録をカテゴリ・予算ゲージ付きでGoogleスプレッドシートに保存・一覧表示する家計簿アプリです。

## セットアップ手順

1. Googleスプレッドシートを新規作成する
2. メニューから「拡張機能」→「Apps Script」を開く
3. デフォルトの `Code.gs` の中身を、このフォルダ内の `gas/Code.gs` の内容で置き換えて保存する
4. 右上の「デプロイ」→「新しいデプロイ」をクリック
   - 種類の選択: 「ウェブアプリ」
   - 実行するユーザー: 自分
   - アクセスできるユーザー: 全員
5. 「デプロイ」をクリックし、表示された **ウェブアプリのURL**（`https://script.google.com/macros/s/.../exec`）をコピーする
6. `index.html` をブラウザで開き、「接続設定」欄に手順5のURLを貼り付けて「保存」をクリックする

これで記録の追加・一覧表示・月次集計がスプレッドシートと連携します。

## 補足

- スプレッドシートには初回アクセス時に自動で `records` シート（ヘッダー: `id, date, category, subCategory, amount, memo`）と `budgets` シート（ヘッダー: `category, subCategory, amount`）が作成されます。
- 記録の編集・削除に対応しています。一覧の各行の「編集」「削除」ボタンから操作してください。
- コードを変更した場合は、Apps Scriptの「デプロイを管理」から新しいバージョンを再デプロイしてください。
